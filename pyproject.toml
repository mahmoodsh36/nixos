[project]
name = "nixos-tools"
version = "0.1.0"
description = "NixOS configuration - not a Python package"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.4",
]

# Prevent setuptools from trying to build this as a package
[tool.setuptools]
py-modules = []
