[project]
name = "nixos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "oumi",
]

[tool.uv.sources]
oumi = { git = "https://github.com/oumi-ai/oumi.git" }
